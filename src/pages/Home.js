import MovieList from '../components/MovieList/MovieList';
import TVList from '../components/TVList/TVList';

const Home = () => {
	return (
		<div>
			<MovieList />
			<TVList />
		</div>
	);
};

export default Home;
